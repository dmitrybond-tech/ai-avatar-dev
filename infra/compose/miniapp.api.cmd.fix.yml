services:
  api:
    working_dir: /app
    command:
      - uvicorn
      - apps.miniapp_api.main:app
      - --host=0.0.0.0
      - --port=8080
      - --proxy-headers
      - --forwarded-allow-ips=*


