services:
  api:
    build:
      context: ../..
      dockerfile: apps/miniapp-api/Dockerfile
    image: local/miniapp-api:dev

  web:
    build:
      context: ../../apps/miniapp-web
      dockerfile: Dockerfile
    image: local/miniapp-web:dev

